/**
 * @format
 */

import {AppRegistry} from 'react-native';
import RouteNavigation from './RouteNavigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => RouteNavigation);
